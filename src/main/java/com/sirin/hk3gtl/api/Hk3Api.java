package com.sirin.hk3gtl.api;



/**
 * HK3GTL 对外 API 入口（占位类）。
 *
 * <h3>职责</h3>
 * <ul>
 *   <li>作为模组暴露给附属 / 其他模组的稳定 API 边界入口</li>
 *   <li>本轮仅建立包边界，后续会逐步补充附属模组扩展能力（事件、注册钩子等）</li>
 *   <li>任何需要被外部模组引用的静态入口，应集中在本类下，避免外部直接触碰 internal 代码</li>
 * </ul>
 *
 * <h3>修改指南</h3>
 * <ul>
 *   <li>新增对外 API：在本类中添加 public static 方法或嵌套接口，严禁暴露 impl 内部类型</li>
 *   <li>添加扩展注册器时：应提供以接口/抽象类为核心的 API，而不是让附属模组依赖具体实现</li>
 *   <li>若 API 形态会变更：请先评估是否会破坏附属模组的兼容性</li>
 *   <li>禁止在本类内做逻辑实现——本类是"门面"，实现放在 common 子模块</li>
 * </ul>
 */
public final class Hk3Api {

    // 私有构造：本类为工具/门面类，禁止外部实例化
    private Hk3Api() {}
}
